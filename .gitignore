# Python
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# Recorded telemetry sessions. Multi-MB captures of a lap — useful locally,
# never in the repo. tests/data/ holds the small fixtures that are tracked.
apps/shaker/recordings/

# Local Claude Code settings
.claude/settings.local.json

# Frontend (Vite + npm). The built bundle in apps/shaker/src/shaker/web/static/
# IS committed so the Pi deploy doesn't need Node — npm install + npm run build
# happen on the Mac.
apps/shaker/frontend/node_modules/
apps/shaker/frontend/tsconfig.tsbuildinfo

# User profile data — generated on first deploy via Ansible template.
apps/shaker/config/profiles.json

# OS
.DS_Store

# Editors
.idea/
.vscode/
*.swp
*~

# Logs
*.log

# FreeCAD backups
*.FCBak
*.FCStd1
